import { text } from "../style"

export default {
    ...text,
    "border": "none",
    "outlineWidth": 0,
    "overflow": "auto",
    "outline": "none",
    "resize": "none",
    "backgroundColor": "inherit",
}

export const select = {
    "opacity": 0.7
}
